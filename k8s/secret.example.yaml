# DO NOT put real credentials here. This file is a template only.
#
# To create the actual secret, run:
#   kubectl create secret generic sheets-sa-key \
#     --from-file=service-account.json=./service-account.json
#
# The structure kubectl creates looks like this:
apiVersion: v1
kind: Secret
metadata:
  name: sheets-sa-key
type: Opaque
data:
  # base64-encoded contents of your service-account.json
  service-account.json: BASE64_ENCODED_JSON_HERE
